# yt-dlp est volontairement non figé : YouTube change souvent,
# il faut la dernière version à chaque build (voir README pour la mise à jour).
# L'extra [default] inclut yt-dlp-ejs (défis JS, nécessite deno) et mutagen.
yt-dlp[default]
# Génère les "PO tokens" exigés par YouTube pour les IP de datacenter,
# via le conteneur sidecar potprovider (voir compose.yaml).
bgutil-ytdlp-pot-provider
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
